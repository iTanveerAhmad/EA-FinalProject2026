# Test profile - uses localhost MongoDB/Kafka (docker-compose)
# Prerequisite: docker-compose up -d mongodb kafka zookeeper
spring:
  data:
    mongodb:
      uri: mongodb://admin:adminpassword@localhost:27017/release_db?authSource=admin
  kafka:
    bootstrap-servers: localhost:9092
